<div class="content-1">
    <div class="uk-container uk-container-center">
        <h2 class="uk-h1">Biežāk lietotie vārdi</h2>
        <div class="clear top-words-wrapper">
            <?php $i = 1 ?>
            <div>
                <?php foreach($words as $day): ?>
                    <div class="top-words uk-panel-box<?php if ($i == 1) { echo ' active'; } ?>">
                        <?php $top = 1 ?>
                        <span class="uk-text-info"><?php echo date($day['date']) ?></span>
                        <?php foreach (unserialize($day['words']) as $word => $count): ?>
                            <span top="<?php echo $top ?>" data-uk-tooltip title="<?php echo $count ?>" class="word"><?php echo $word ?></span>
                            <?php $top++ ?>
                        <?php endforeach; ?>
                    </div>
                    <?php $i++ ?>
                <?php endforeach; ?>
            </div>
            <button class="uk-button prev" type="button" disabled>< Atpakaļ</button>
            <button class="uk-button next" type="button">Tālāk ></button>
        </div>
    </div>
</div>